// App.tsx
// Minimal React TSX component source. This is a source file only — you can compile with your preferred bundler.
import React from 'react';

export function App() {
  return (
    <div style={{ color: '#e5e7eb', padding: 12 }}>
      <h3 style={{ color: '#22d3ee' }}>Singularity Engine TSX Demo</h3>
      <p style={{ color: '#9ca3af' }}>This is a minimal TSX component. Use your bundler to compile.</p>
    </div>
  );
}
